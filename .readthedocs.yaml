version: 2

build:
  os: ubuntu-lts-latest
  tools:
    python: "3.14"
  jobs:
    post_checkout:
      - git lfs install
      - git lfs pull

mkdocs:
  configuration: mkdocs.yml

python:
   install:
   - requirements: requirements.txt
